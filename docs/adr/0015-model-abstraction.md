# ADR-0015: Model abstraction layer (merged)

## Status

Superseded.

## Context

ADR-0015 was originally proposed as a standalone record for the model abstraction layer. ADR-0014 was subsequently created to cover the same material, and ADR-0015 was merged into it to avoid duplication.

## Decision

This ADR is superseded by [ADR-0014](./0014-model-abstraction.md). All decisions previously recorded here are now captured in ADR-0014.

## See also

- [ADR-0014: Model abstraction layer for model-swapping milestone](./0014-model-abstraction.md)
