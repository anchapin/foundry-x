{
  "name": "example_skill",
  "version": "0.1.0",
  "description": "Example skill demonstrating skill JSON schema. Not for production use. This toy skill accepts a name and returns a personalised greeting, serving as a minimal template for skill authors.",
  "input_schema": {
    "type": "object",
    "properties": {
      "name": {
        "type": "string",
        "description": "The name of the person to greet."
      },
      "enthusiasm": {
        "type": "integer",
        "minimum": 1,
        "maximum": 5,
        "default": 3,
        "description": "Enthusiasm level from 1 (muted) to 5 (maximum). Controls the number of exclamation marks in the greeting."
      }
    },
    "required": ["name"],
    "additionalProperties": false
  },
  "output_schema": {
    "type": "object",
    "properties": {
      "greeting": {
        "type": "string",
        "description": "The personalised greeting string."
      }
    },
    "required": ["greeting"]
  }
}
